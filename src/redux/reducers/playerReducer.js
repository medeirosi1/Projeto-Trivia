const initialState = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

export default (state = initialState, { type, payload }) => {
  switch (type) {
  default:
    return state;
  }
};
